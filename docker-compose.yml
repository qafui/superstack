version: '2'

services:
    backend:
        build: .
        ports:
            - 8000:8000
            - 9229:9229 #debugger port
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        environment:
            PORT: 8000
        command: npm run start:docker
